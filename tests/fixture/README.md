# fixture

A tiny repository the package's dry run points opencode at. Its test script fails on purpose, so a run that ever got as far as executing it would be visible.

Run the tests with `bash test.sh`.
